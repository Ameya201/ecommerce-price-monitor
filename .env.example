# Optional — only needed for real MySQL connection
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=price_monitor

# Database mode: 'sqlite' (default) or 'mysql'
DB_MODE=sqlite
